import 'package:flutter/material.dart';

class ScreenCharacterDetail extends StatelessWidget {
  const ScreenCharacterDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}