1. Neuer Battle Result Screen wird nicht bei Hostsieg angesprungen — **GEFiXT** (Doppeltes `Navigator.pop` entfernt nach `onGameOver`-Callback)
2. Focus Fire bei Spieler verhält sich seltsam. Etwa, wenn ein Spielertoken angegriffen hat und ein anderer Spielertoken wählt "Focus Fire" kommt nur, dass der Angriff nicht möglich ist. — **GEFiXT** (Drei Ursachen: (a) `getAvailableActions` zeigte FocusFire auch an, wenn die Einheit bereits gehandelt hatte → jetzt nur noch für Einheiten, die noch nicht gehandelt haben. (b) `_enterFocusFireTargetingMode` zeigte ALLE Gegner als Ziel an, auch wenn kein Angreifer sie erreichen konnte → jetzt werden nur erreichbare Gegner angezeigt (Nahkampf ODER Fernkampf). (c) `performFocusFire` war hart auf `CombatAction.melee` codiert → jetzt wählt jeder Angreifer automatisch die beste Aktion: Nahkampf (distance <= 1) oder Fernkampf (distance <= rangeValue))
3. Karte im ScreenMain ist wieder linkszentriert, obwohl sie mittig sein sollte. — **GEFiXT** (`_centerMap()` wird nur noch einmal beim ersten Build aufgerufen, statt bei jedem `setState`)
